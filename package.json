{
  "name": "governed-action-lab",
  "version": "0.2.0",
  "description": "A deterministic reference implementation for governing agent actions.",
  "type": "module",
  "private": false,
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "governed-action-lab": "./dist/src/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "npm run build --silent && node --test dist/test/*.test.js",
    "eval": "npm run build --silent && node dist/src/eval-runner.js",
    "contract:check": "node scripts/check-context-contract.mjs",
    "fixture:fresh": "npm run build --silent && node scripts/freshen-public-fixture.mjs",
    "demo:sync": "npm run build --silent && node scripts/build-browser-runtime.mjs && node scripts/sync-demo.mjs",
    "demo:check": "npm run build --silent && node scripts/build-browser-runtime.mjs --check && node scripts/sync-demo.mjs --check",
    "proof:sync": "npm run build --silent && node scripts/build-portable-proof.mjs",
    "proof:check": "npm run build --silent && node scripts/build-portable-proof.mjs --check",
    "qa:responsive": "node scripts/check-responsive.mjs",
    "public-safety:check": "node scripts/check-public-safety.mjs",
    "public-safety:install": "git config core.hooksPath .githooks",
    "check": "npm run public-safety:check && npm run contract:check && npm run typecheck && npm test && npm run eval && npm run demo:check && npm run proof:check && npm run qa:responsive",
    "action": "npm run build --silent && node dist/src/cli.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/node": "24.10.1",
    "esbuild": "0.28.1",
    "typescript": "5.9.3"
  }
}
